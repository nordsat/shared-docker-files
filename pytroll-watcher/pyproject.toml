[project]
name = "pytroll-watcher"
version = "0.1.0"
description = "pytroll-watcher instance"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytroll-watchers[minio,local,publishing,ssh,dataspace,datastore,s3]>=0.10.0",
]
